import fs from 'node:fs';import path from 'node:path';import {loadConfig} from '../src/config.js';
const c=loadConfig();const errors=[],warn=[];if(c.securityMode!=='mainnet')warn.push('SECURITY_MODE is not mainnet');if(!c.explicitWalletPassword)errors.push('WALLET_PASSWORD must be explicitly set');if(!c.explicitNodeKeyPassword)errors.push('NODE_KEY_PASSWORD must be explicitly set');if(c.autoBootstrapValidators)errors.push('AUTO_BOOTSTRAP_VALIDATORS must be false');if(c.allowUntrustedTestnetFastSync)errors.push('ALLOW_UNTRUSTED_TESTNET_FAST_SYNC must be false');if(c.autoInstallOllama)errors.push('AUTO_INSTALL_OLLAMA must be false');if(!c.apiToken||c.apiToken.length<24)errors.push('API_TOKEN must be at least 24 chars');if(!c.constitutionEnabled)errors.push('CONSTITUTION_ENABLED must be true');if(!c.trustedCheckpointValidatorRoot)warn.push('TRUSTED_CHECKPOINT_VALIDATOR_ROOT not configured');const manifest=path.resolve(process.env.NETWORK_MANIFEST||'network-manifest.json');if(!fs.existsSync(manifest))errors.push(`network manifest missing: ${manifest}`);const out={ok:errors.length===0,errors,warnings:warn,networkId:c.networkId,protocolVersion:c.protocolVersion,manifest};console.log(JSON.stringify(out,null,2));if(errors.length)process.exit(2);
