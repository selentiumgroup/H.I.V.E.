import fs from 'node:fs';import crypto from 'node:crypto';import path from 'node:path';
const [file,expectedArg]=process.argv.slice(2);if(!file)throw new Error('usage: npm run verify-release -- <file> [sha256|sha256file]');const hash=crypto.createHash('sha256').update(fs.readFileSync(file)).digest('hex');let expected=expectedArg||'';if(expected&&fs.existsSync(expected))expected=fs.readFileSync(expected,'utf8').trim().split(/\s+/)[0];if(expected&&hash.toLowerCase()!==expected.toLowerCase()){console.error(`FAIL ${path.basename(file)}\nactual   ${hash}\nexpected ${expected}`);process.exit(2);}console.log(`OK ${path.basename(file)} ${hash}`);
